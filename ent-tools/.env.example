# MarkItDown API base URL (for n8n or local scripts calling the API).
# No trailing slash. Set in n8n variables as MARKITDOWN_API_URL or tools.markitdown.url.
MARKITDOWN_API_URL=http://localhost:8080
