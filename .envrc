# Load Doppler secrets when you cd into this directory (requires direnv).
# First time: run `direnv allow` in this directory.
# See https://docs.doppler.com/docs/direnv
set -a
source <(doppler secrets download --no-file --format env)
set +a
